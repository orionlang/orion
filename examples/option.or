type Option = | None | Some(i32)

fn divide(a: i32, b: i32) Option {
    if b == 0 {
        return None
    } else {
        return Some(a / b)
    }
}

fn main() i32 {
    let result = divide(10, 2)
    return match result {
        None => 0,
        Some(x) => x,
    }
}
